const gameItem = {
  buttonTitle: "Game",
  fields: [{ title: "New", action: "restart-game" }],
}

const optionsItem = {
  buttonTitle: "Options",
  fields: {},
}

const helpItem = {
  buttonTitle: "Help",
  fields: {},
}

export const menuData = [gameItem, optionsItem, helpItem]
